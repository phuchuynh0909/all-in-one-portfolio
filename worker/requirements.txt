# Common
requests>=2.31.0

# Bytewax streaming framework
bytewax>=0.19.0
bytewax-clickhouse>=0.0.1

# Data processing
pandas>=2.2.1
pyarrow>=14.0.0
numpy>=1.24.0
orjson>=3.9.0

# ClickHouse connectivity
clickhouse-connect>=0.6.0

# Workflow orchestration
prefect>=2.0.0

# Configuration
python-dotenv>=1.0.0

# HTTP client for Telegram notifications
aiohttp>=3.8.0

# MQTT for real-time data ingestion
paho-mqtt>=1.6.0

# Optional: Development and testing
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
vectorbt>=0.26.2
# DNSE OpenAPI market-data WebSocket source (infra/dnse_ws_input.py).
# The source drives the official SDK vendored at worker/dnse_sdk — the source
# itself is committed here, but its third-party dependencies are installed from
# this file. msgpack backs DNSE_WS_ENCODING=msgpack; urllib3 (used by the SDK's
# REST client) arrives with requests above.
websockets>=12.0
certifi>=2024.0.0
msgpack>=1.0.0
