[pytest]
asyncio_mode=auto
markers =
    real_auth: exercise the real authentication guard instead of the stubbed user
    slow: takes long enough that you may want to deselect it
    integration: needs a live external service (skipped when unreachable)
