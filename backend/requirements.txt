fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.3.4
python-multipart>=0.0.9
loguru>=0.7.2
orjson>=3.10.0
httpx>=0.27.0
pytest>=8.2.0
pytest-asyncio>=0.23.7
pycryptodomex>=3.19.0
requests>=2.31.0
protobuf>=4.25.0

# Database
sqlalchemy>=2.0.28
alembic>=1.13.1
python-dotenv>=1.0.1
clickhouse-connect>=0.6.0

# Data processing
pandas>=2.2.1
tabulate>=0.9.0  # pandas.DataFrame.to_markdown() backend
numpy>=1.26.4
deltalake>=0.15.3
pyarrow>=15.0.1
numba>=0.59.0
ta-lib>=0.4.28
ta>=0.11.0
numba>=0.61.0
tables>=3.9.0

# Machine Learning
xgboost>=2.0.3
lightgbm>=4.3.0
catboost>=1.2.10
scikit-learn>=1.4.1
optuna>=4.0.0
stable-baselines3>=2.3.0
gymnasium>=0.29.1

# Utilities
python-dateutil>=2.9.0
pykalman>=0.9.5
vectorbt>=0.26.2
fastapi-cache2>=0.2.1

# Portfolio optimization
PyPortfolioOpt>=1.5.4
scipy>=1.10.0
statsmodels>=0.14.6

# Task orchestration
prefect>=2.14.0

# RAG pipeline (report PDF -> markdown -> embeddings -> Qdrant)
qdrant-client>=1.9.0       # embeddings come from Qwen3-Embedding-8B, not fastembed
ollama>=0.3.0              # Ollama Python client (RAG_EMBED_BACKEND=ollama)
sentence-transformers>=3.0 # local in-process embedder (RAG_EMBED_BACKEND=huggingface)
transformers>=4.56.0       # Qwen3 support + from_pretrained(dtype=) for the local embedder
marker-pdf>=1.2.0          # PDF->markdown parser option "marker" (pulls torch + models; heavy)
llama-parse>=0.5.0         # PDF->markdown parser option "llamaparse" (cloud API; needs LLAMA_CLOUD_API_KEY)

# TradingAgents (vendored at ../vendor/TradingAgents, put on sys.path by
# app.services.tradingagents). These are its runtime deps; the multi-agent
# graph runs against a local Ollama server (see app/services/tradingagents/README.md).
langgraph>=0.4.8
langgraph-checkpoint-sqlite>=2.0.0
langchain-core>=0.3.81
langchain-openai>=0.3.23
langchain-google-genai>=2.0     # only needed when TRADINGAGENTS_LLM_PROVIDER=google/gemini
langchain-anthropic>=0.3.15     # only needed when TRADINGAGENTS_LLM_PROVIDER=anthropic/claude
stockstats>=0.6.5
ddgs>=6.0            # keyless web search for the news/sentiment analysts (Tavily optional via TAVILY_API_KEY)
# yfinance intentionally omitted: this deployment uses Vietnamese-market data
# only. The vendored framework imports yfinance at module load, so
# app.services.tradingagents installs a lightweight stub when it is absent
# (Yahoo is never actually called — all data routes to the VN 'portfolio' vendor).

-e ./libs/backtesting.py
